#pragma once
#include "ProcessThread.h"
//not used yet, may not need to separate threads
class CAutoProcessThread
	: public CProcessThread
{
public:
	CAutoProcessThread(void);
	~CAutoProcessThread(void);
};
