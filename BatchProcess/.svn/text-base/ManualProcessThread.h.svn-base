#pragma once
#include "ProcessThread.h"
class CManualProcessThread
	: public CProcessThread
{
public:
	CManualProcessThread(void);
	~CManualProcessThread(void);
protected:
	
public:
	void SetCategory(int iLeadType, int iOrderType);
	void ProcessBatch(void);
protected:
	
};
